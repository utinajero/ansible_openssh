ADD: var_files to your PLAYBOOK.yml and point it to the openssh role vars directory. 
Defaults to using RedHat vars. 
i.e. sample_playbook.yml.

Tested on CentOS 6.5 x86_64.
It should also work on Ubuntu 12.04 LTS.
